# 安装与使用

本文件夹是一套可直接复制的 `project-memory-init` Skill。根目录的 `SKILL.md` 是唯一可被 Skill 扫描器发现的入口；`references/` 保存初始化规范，`assets/` 保存生成到目标项目的中文模板。

## 安装

把整个 `project-memory-init/` 文件夹复制到用户级 Skill 根目录：

```text
~/.agents/skills/project-memory-init/
```

若所用客户端只扫描 Codex 专用目录，也可以放到 `~/.codex/skills/project-memory-init/`。同一客户端环境只保留一个安装位置，避免重复发现。

更新已安装版本前先比较现有文件；保留用户对 Skill 的定制，不直接覆盖无法确认来源的修改。

## 使用

打开尚未建立项目记忆的项目根目录，对代码智能体说：

> 使用 `project-memory-init` 初始化当前项目。

Skill 会调查目标项目，并在目标仓库生成：

- `project-memory/` 中文 Obsidian 项目记忆；
- `.agents/skills/project-memory/SKILL.md` 项目本地日常 Skill；
- 根 `AGENTS.md` 中稳定的 Project Memory 入口和内容分流规则；
- `CLAUDE.md`、`GEMINI.md` 与 `.github/copilot-instructions.md` 等厂商薄入口；
- 初始化工作流和第一篇只追加工作记录。

初始化完成后，普通任务接管、事实刷新和收尾使用目标项目生成的 `project-memory` Skill，不重复运行初始化。

目标仓库已经存在 `AGENTS.md`、`CLAUDE.md`、`GEMINI.md` 或 Copilot 指令时，初始化只在原文件中补齐 Project Memory 入口，不创建替代文件或覆盖已有规则；对应入口不存在时才创建最小文件。

## 分发边界

- 复制整个文件夹，不单独复制根 `SKILL.md`，否则会缺少规范和模板。
- `assets/日常项目记忆Skill模板.md` 是生成资产，不是已安装 Skill；保持该文件名可避免扫描器把占位模板误识别为第二个 `project-memory` Skill。
- 模板中的 `〔……〕` 必须由智能体替换成目标项目事实，不能直接交付。
