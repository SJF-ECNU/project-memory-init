Follow the repository-wide agent instructions in `AGENTS.md`.

For meaningful repository work, start from `project-memory/首页.md` and follow `project-memory/协议.md`. Use the repository-local `project-memory` Skill when supported; otherwise follow the protocol directly.
