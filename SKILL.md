---
name: project-memory-init
description: 在新仓库或项目记忆明显不完整的仓库中，为多个不同代码 Agent 一次性初始化中文、兼容 Obsidian、可跨 Agent 接续的项目记忆，并生成项目本地的日常 project-memory Skill。用户需要让不同 Agent 快速同步项目背景、当前进度、注意事项、部署信息或下一步时使用；普通任务接管和收尾使用目标仓库内的 project-memory Skill。
---

# 项目记忆初始化

为目标项目建立一套随仓库版本控制、以仓库根目录为 Obsidian Vault、与智能体厂商无关的项目记忆，让不同代码 Agent 无需依赖原始对话即可快速同步项目背景、当前进度、关键注意事项和下一步。初始化只建立接管基础设施，不执行部署、修改外部系统或猜测未知环境事实。

## 边界

- 这是一次性建库或缺口修复 Skill。核心入口已经完整存在时，转用目标仓库的 `.agents/skills/project-memory/SKILL.md`。
- 初始化授权只覆盖目标仓库内的文档、项目本地 Skill 和必要的智能体入口指针。不得借机修改应用行为、生产配置、凭据、远端服务或 Git 历史。
- 已有文件采用保留式更新：先分析差距与冲突，再做最小修改。工作记录只追加；既有工作流状态、其他智能体规则和用户改动不覆盖、不重置。

## 执行

### 1. 只读调查

1. 确认目标项目根目录和 Git 状态；若不是 Git 仓库，也可初始化，但元数据使用 `unavailable`。
2. 检查 `project-memory/`、`.agents/skills/project-memory/`、`AGENTS.md`、`CLAUDE.md` 等现有入口，判断是全新初始化、缺口修复还是已经完成。
3. 完整阅读适用于仓库根的智能体说明文件。若说明引用更具体的架构、部署、安全或发布资料，读取与项目事实相关的原文。
4. 调查主要项目来源：README、清单与锁文件、目录结构、架构/设计、环境配置示例、容器与 IaC、CI/CD、运行手册、测试入口和当前变更系统。存在 `.codegraph/` 时，优先用 CodeGraph理解代码结构；没有时使用清单、目录和有界源码调查。
5. 区分“静态文档事实”“当前 Git/配置事实”“已实时核验事实”和“未知”。发现矛盾时记录双方、采用依据和待核验项。

完成标准：能够列出项目目标、主要模块、当前能力和非目标、关键开发约束、环境/部署拓扑、权威来源和所有未解决冲突。没有资料的字段明确写“未在仓库中登记”，不推测。

### 2. 规划初始化

完整阅读 [初始化规范](references/初始化规范.md)，根据其中的交付物和安全边界制定有界计划。目标项目已经使用 OpenSpec 或其他变更协议时遵循它；项目没有时只使用普通计划，不为初始化强行引入新的规范系统。

开始写入前说明：初始化模式、将创建或更新的文件、保留的现有内容、来源冲突和仍未知的事实。只有缺少用户选择会实质改变结构或覆盖内容时才停下来询问。

### 3. 生成项目记忆

使用 `assets/项目记忆/` 作为结构模板，而不是事实来源。将所有 `〔……〕` 占位内容替换为目标项目的已核验事实；不适用的章节写明“不适用”或删除，不能留下占位符。

在目标仓库创建或补齐：

- `project-memory/首页.md`
- `project-memory/协议.md`
- `project-memory/项目概览.md`
- `project-memory/项目背景与当前边界.md`
- `project-memory/开发与运维注意事项.md`
- `project-memory/环境与部署.md`
- `project-memory/模板/工作流模板.md`
- `project-memory/模板/工作记录模板.md`
- `project-memory/工作流/项目记忆初始化.md`
- `project-memory/工作记录/YYYY/MM/<时间>-<智能体>-项目记忆初始化.md`

仓库根目录是 Vault。双链从 Vault 根编写，使项目记忆可以引用 `docs/`、源码和智能体说明；不要把 `project-memory/` 子目录单独设计成 Vault。

生成的首页必须保留 Obsidian 记忆图谱提示：全局图谱使用 `path:"project-memory/" -path:"project-memory/模板/"` 聚焦当前记忆，并建议隐藏孤立与未创建笔记、开启方向箭头、按首页/工作流/工作记录分组。默认不创建或覆盖 `.obsidian/`；只有用户明确要求配置当前 Vault 时，才保留式修改图谱设置，不得重置主题、插件或工作区偏好。

### 4. 生成日常 Skill 与入口指针

把 `assets/日常项目记忆Skill模板.md` 适配后写入目标仓库 `.agents/skills/project-memory/SKILL.md`。日常 Skill 负责接管、事实刷新和收尾，不重复初始化。

在目标仓库已有的适用智能体说明中加入一条简短指针，说明开始/继续有意义的工作和收尾时使用项目本地 `project-memory` Skill。若仓库没有任何通用智能体入口，可创建最小 `AGENTS.md`，仅包含项目记忆指针；不得把整套协议复制到入口文件。

### 5. 初始化记录

初始化工作流记录目标、当前状态、最新结果、阻塞、下一步和按时间由近到远排列的至多五篇近期工作记录链接。第一篇工作记录按模板填写真实时间、时区、智能体、客户端、模型、会话、Git 分支和提交信息；无法获取的字段使用 `unknown`、`unavailable` 或 `null`，不推测。

### 6. 验收

执行与目标仓库工具链相称的检查，至少验证：

1. 必需文件和项目本地 Skill 均存在，所有 `〔……〕` 占位内容已经处理。
2. Obsidian 双链和相对 Markdown 链接能够从仓库根解析。
3. 项目记忆未包含凭据值、私钥、完整环境文件、敏感请求/响应或原始命令输出。
4. 已有用户文件和智能体规则没有被覆盖，Git 差异只包含初始化范围。
5. 使用“读取首页、相关工作流和最近一篇工作记录，信息不足时再按需追溯相关历史与权威来源”的方式模拟接管，能够回答项目是什么、当前状态、重要边界、环境/部署、阻塞和下一步。
6. Skill 校验器可用时验证项目本地 Skill；项目使用 OpenSpec 或格式化工具时运行对应检查。

验收完成后，把初始化工作流标记为 `completed` 并创建只追加工作记录。最终回答说明生成位置、主要来源、未知或冲突、验证结果，以及今后使用项目本地 `project-memory` Skill 的方式。
