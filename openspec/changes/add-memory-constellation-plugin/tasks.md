## 1. 规格与工程

- [x] 1.1 新增 OpenSpec 提案、设计、任务和可验收规范。
- [x] 1.2 搭建独立 Obsidian 插件构建与测试工程。

## 2. 关系模型

- [x] 2.1 实现 Vault 范围过滤、节点分类、标题缩短和链接折叠。
- [x] 2.2 实现稳定的 2D 语义布局和聚焦邻域。
- [x] 2.3 为模型的分类、过滤、标题与确定性布局编写聚焦测试。

## 3. 视图与交互

- [x] 3.1 实现 Canvas 渲染、曲线连接、光晕、渐进标签与克制动画。
- [x] 3.2 实现聚焦/全景、范围切换、搜索、缩放、拖拽和打开笔记。
- [x] 3.3 注册自定义 View、命令和 Ribbon 入口，并在 Vault 元数据变化后防抖刷新。

## 4. 交付与验证

- [x] 4.1 补充插件安装与视觉交互说明。
- [x] 4.2 运行类型检查、模型测试、生产构建、OpenSpec 校验和格式检查。
- [x] 4.3 将生产产物安装到 EvoMind Vault，验证真实笔记数、链接数、视图加载和核心交互。

## 5. 悬停交互与视觉校准

- [x] 5.1 悬停节点时临时突出一跳关系与标签，移开后恢复锁定焦点。
- [x] 5.2 去除星空纹理、呼吸光效、详情卡和图例，改为中性深灰与克制关系高亮。
- [x] 5.3 重新构建并在 EvoMind 中实测悬停、点击锁定和视觉层级。

## 6. 移出悬停后恢复全图

- [x] 6.1 仅由鼠标悬停控制关系高亮；未悬停时所有节点恢复正常亮度。
- [x] 6.2 验证空白处、离开画布、点击、归心与数据刷新不会重新锁定中心高亮。
- [x] 6.3 构建并更新 EvoMind 中的插件，验证实际画面。

## 7. 材质与动画升级

- [x] 7.1 统一石墨背景、矿物色节点、细腻边缘反射和轻量浮动工具栏。
- [x] 7.2 实现悬停进入、节点切换、移出恢复的连续过渡与关系线渐显。
- [x] 7.3 优化标签避让、视图留白和搜索导航，静止后停止无效重绘。
- [x] 7.4 在真实 Vault 中验证外观和交互，完成减少动态效果与退出悬停的回归检查。
- [x] 7.5 修复密集文档中大节点丢失名称的问题，并验证六个文档分类在悬停时均显示。

## 8. 中性底色、分层配色与玻璃风格

- [x] 8.1 保留黑灰背景与控件，节点按香槟核心、雾蓝工作流、灰紫记忆与蓝灰记录区分层级。
- [x] 8.2 为浮动控件增加透明玻璃层、边缘反光、指针跟随高光与克制回弹，兼容减少动态效果。
- [x] 8.3 回归悬停恢复、必要标签和静止停止重绘，构建并更新 EvoMind 中的插件。
- [x] 8.4 修复搜索聚焦时宿主样式造成的灰色矩形，保留胶囊级焦点提示。
- [x] 8.5 为主要节点增加透色玻璃内层与边缘反光，小节点优先保证颜色和轮廓可见。
- [ ] 8.6 在真实窗口中验收最终配色、搜索聚焦状态和悬停退出效果。

2026-09-05：19 项测试、类型检查、构建和 OpenSpec 校验通过；EvoMind 插件文件与构建产物逐文件一致且已重新启用。最终实机验收未完成：窗口控制出现超时、`noWindowsAvailable`，随后返回的内容仍停留在旧的新标签页，不能据此确认新版画面。

## 9. 幕布柔光与玻璃透光

- [x] 9.1 在图谱背后增加跟随鼠标的中性柔光层，跨越画布与搜索控件连续移动。
- [x] 9.2 搜索胶囊接收底层柔光并加强背景模糊，输入文字保持清晰。
- [x] 9.3 验证离开幕布、取消指针、窗口失焦、减少动态效果与触屏时不会残留柔光或影响图谱焦点。
- [x] 9.4 构建、安装并检查实际显示。
- [x] 9.5 将光斑从 360px 收缩至 160px，替换双层渐变为抖色高斯光纹，减轻暗部色带。
- [x] 9.6 添加独立按需动画：短程缓动、速度相关形变、停止后恢复圆形并停止调度；不驱动图谱重绘。
- [ ] 9.7 补充实际搜索框上方移动的完整动画验收。

2026-09-05：24 项测试、生产构建、OpenSpec 和格式检查通过；新版已安装并重新启用。真实截图确认 56 节点 / 131 关系正常显示、小光斑已呈现。继续检查搜索框时检测到用户正在操作应用，停止鼠标自动控制；搜索模糊的完整实机动画尚未验收，不将静态截图当作动态效果证据。

## 10. 切换时工具栏尺寸稳定

- [x] 10.1 固定范围与标签模式按钮的栏位，搜索区只随窗口宽度调整，不随按钮文字变化。
- [ ] 10.2 回归四种切换组合与窄窗口布局，更新 EvoMind 并检查胶囊边界。

## 11. 可定位的笔记搜索

- [x] 11.1 按标题、原始文件名与路径匹配多个关键词，标题精确匹配优先，不读取笔记正文。
- [x] 11.2 输入即展示结果、类型和路径，明确当前范围与空结果；下拉浮层不改变胶囊尺寸。
- [x] 11.3 支持方向键选择、Enter 定位、⌘/Ctrl+Enter 打开、Escape 关闭，兼容中文输入法。
- [x] 11.4 范围切换及 Vault 更新后刷新搜索结果；补充模型与交互测试，构建并更新 EvoMind。

2026-09-05：34 项测试、类型检查与生产构建通过，覆盖查询排序、多个关键词、范围变化与文件移除、空结果、中文输入法、键盘/点击定位、短画布列表高度及四种开关组合。EvoMind 中实际搜索“部署”展示 4 条带类型、完整路径的结果，静态截图确认下拉浮层与胶囊显示正常。随后检测到用户操作，停止鼠标控制；四种组合的实机尺寸对照、真实键盘及中文输入法交互仍未完成验收。最后通过 Obsidian CLI 重新加载最新产物，插件状态 enabled=true；安装文件与构建产物逐文件一致。

## 12. 范围过渡与生长重播

- [x] 12.1 用约一秒内完成的关系层级生长替代整图同时淡入；节点短距舒展、关系线逐段延伸、标签稍后出现。
- [x] 12.2 范围切换保留已有节点，从当前画面连续衔接，移除节点短暂淡出；快速切换不会闪回或遗留节点。
- [x] 12.3 在右下角加入独立“重播生长动画”按钮，从空白播放当前范围，不改变搜索工具栏布局。
- [ ] 12.4 回归减少动态效果、空图、动画中操作、静止停止重绘和退出清理，构建并在 EvoMind 验证。

2026-09-05：动画回归覆盖层级生长、1000 节点深链的有界时长、快速反向衔接、离场节点只绘制不命中、重播从空白开始、减少动态效果、静止停止重绘和退出释放。已构建安装并通过 CLI 重载；实际 UI 出现“重播生长动画”按钮，点击范围开关后运行状态为 219 节点 / 435 关系，工具栏宽度仍为 372px。完整动态观感未验收：窗口工具缓存旧画面并出现 noWindowsAvailable，进一步只读检查发现 Obsidian document.hidden=true，RAF 已被宿主暂停；这不能当作前台动画完成或帧率证据。

## 13. 全局重播放慢至 10 秒

- [x] 13.1 仅全局重播使用 10 秒时间轴，按实际关系层级时长归一化；范围切换保持短过渡。
- [x] 13.2 验证 9.9 秒仍在播放、10 秒完成，两种范围均适用；保留减少动态效果与退出清理。
- [x] 13.3 构建并更新 EvoMind 插件，同步使用说明。

2026-09-05：44 项测试、类型检查、生产构建、OpenSpec 和格式检查通过。两种范围均在模拟时钟 9.9 秒保持播放、10 秒完成；普通范围过渡仍在 0.9 秒内结束。EvoMind 安装产物与构建一致，已重载并打开视图；只读检查确认实际加载的重播入口传入 1e4（10000ms）。本次没有新增完整 10 秒前台录像验收。

## 14. 点击锁定与关联笔记侧栏

- [x] 14.1 单击锁定、再次单击同一节点取消、双击打开文件；无选中状态时保留悬停移出恢复全图，拖拽不误选。
- [x] 14.2 选中关系优先于临时悬停，支持空白处、Escape 与侧栏关闭按钮取消；范围或 Vault 刷新清理失效选中项。
- [x] 14.3 展开直接关联笔记侧栏，显示完整标题、类型和路径，支持列表筛选、定位与独立打开；不移动画布布局或遮住首次点击点。
- [ ] 14.4 补充事件序列、侧栏、密集关系、搜索/刷新/重播与悬停回归，构建安装并检查真实 Vault。

本轮按用户最新要求调整第 6 节的交互：只有未选中节点时才在悬停结束后恢复全图；用户明确点击选中后保持锁定，不自动锁定首页。

2026-09-05：51 项测试、类型检查、构建、OpenSpec 与格式检查通过，覆盖锁定/取消、双击事件序列、拖拽不误选、100 条直接关联的完整去重列表、筛选与独立打开、侧栏方位保持、Git/Vault/范围失效清理、10 秒重播及锁定后停止重绘。已安装到 EvoMind 并重载，产物逐文件一致；只读运行状态确认侧栏组件已创建且初始未锁定。真实前台点击及侧栏截图未验收：document.hidden=true，UI 控制返回 elementHasNoFrame，唤起前台尝试未成功；未将单元测试或已加载组件当作视觉验收。

## 15. 插件分支独立发布

- [x] 15.1 确认主分支尚未纳入插件，保留全部本地插件改动并建立 `memory-constellation` 分支。
- [x] 15.2 补充分支首页、安装说明和分工；依赖与构建产物保持忽略，不改动已安装的 Vault。
- [x] 15.3 重新运行测试、类型检查、生产构建和 OpenSpec 校验，检查实际提交文件。
- [x] 15.4 推送插件分支；主分支只增加可选插件入口，核对远端默认分支与文件范围。

2026-09-05：通过 `npm ci` 重新安装依赖，51 项测试、类型检查、生产构建、OpenSpec 与提交格式检查通过。已向公开仓库推送 `memory-constellation` 分支；GitHub API 确认默认分支仍为 `main`，其目录只有原有 Skill、规范、模板、说明和 MIT 许可，本次仅在 README 增加两行可选插件入口。插件分支含源码、测试与安装说明，不含依赖目录或生成的 `main.js`。未改动 EvoMind 的安装，未创建 GitHub Release 或进行商店上架；先前未完成的实机视觉验收仍保持待办。
